/**
 * Εικόνες προϊόντων στα email: καμία.
 *
 * ── Γιατί ──────────────────────────────────────────────────────────────────
 *
 * Ο κατάλογος είναι όλος **WebP** στο CDN και δεν υπάρχει δεύτερη μορφή: το
 * `451_1.jpg` επιστρέφει 404, και ο Bunny Optimizer δεν είναι ενεργός — το
 * `?format=jpg` γυρίζει πάλι `image/webp`.
 *
 * Το Outlook για Windows αποδίδει με τη μηχανή του Word και δεν εμφανίζει
 * WebP: στη θέση της φωτογραφίας μπαίνει το εικονίδιο σπασμένης εικόνας. Στο
 * Gmail φαίνεται κανονικά, γι' αυτό περνά απαρατήρητο στις δοκιμές και σπάει
 * ακριβώς στα εταιρικά γραμματοκιβώτια.
 *
 * Το λογότυπο δεν έχει το πρόβλημα — είναι PNG στο `web.kolleris.com` και
 * φορτώνει παντού.
 *
 * ── Τι βλέπει ο παραλήπτης ─────────────────────────────────────────────────
 *
 * Τα templates έχουν ήδη `{{#if image}}`: χωρίς εικόνα μπαίνει ένα ουδέτερο
 * γκρι πλαίσιο στη θέση της. Καθαρό κενό, όχι σπασμένο εικονίδιο.
 *
 * ── Πώς επανέρχονται ───────────────────────────────────────────────────────
 *
 * Μόλις υπάρξει JPEG ή PNG για τον κατάλογο —Bunny Optimizer, ή δεύτερη
 * παραγωγή αρχείων— αυτή η συνάρτηση επιστρέφει ξανά τη διεύθυνση και τα
 * τέσσερα email που δείχνουν είδη τη βρίσκουν αμέσως. Ένα σημείο, όχι
 * τέσσερα.
 */
export function mailProductImage(_imageUrl: string | null | undefined): string {
  return "";
}
